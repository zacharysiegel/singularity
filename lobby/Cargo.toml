[package]
name = "lobby"
version = "0.1.0"
edition = "2024"

[dependencies]
# todo: crypt standalone package
shared = { path = "../shared" }

log = { workspace = true }
env_logger = { workspace = true }
actix-web = { workspace = true }
